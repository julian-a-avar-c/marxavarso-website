---
import Layout from "@/layouts/Layout.astro";

import { config as mainConfig } from "@/config/main";
import { config as pageConfig } from "@/config/pages/index";

import { Icon } from "astro-icon/components";
import Header from "@/components/layout/Header.astro";
import { Button } from "@/components/ui/button";

import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { Separator } from "@/components/ui/separator";
import Halfbleed from "@/components/section/Halfbleed.astro";
import { Accordion } from "@/components/my-ui/accordion";
import type { Item } from "@/config/pages/products";
import { cn } from "@/lib/utils";

type Props = {
  reverse?: boolean;
  class?: string;
};
const { class: className, reverse } = Astro.props;
---

<!-- TODO: Use Content80 -->
<section class={cn("col-[content]", "flex justify-center")}>
  <div
    class={cn(
      "flex flex-col 2xl:grid 2xl:grid-cols-2 gap-16",
      "w-[80%]",

      //
      "[&>img]:rounded-2xl",

      "[&>div]:flex [&>div]:flex-col",
      "[&>div]:gap-4",
      // "[&>div]:bg-cover [&>div]:bg-center",

      //
      "[&>div>h2]:text-5xl [&>div>h2]:font-bold [&>div>p]:text-lg",
      "direc",
    )}
  >
    <slot name="slot-1" />
    <slot name="slot-2" />
    <!-- <img
      src={image.src}
      alt={image.alt}
      class={cn("object-cover object-center", "row-start-1", {
        "col-start-2": reverse,
      })}
    /> -->

    <!-- <div
      class={cn("flex flex-col gap-4", "row-start-1", {
        "col-start-1": reverse,
      })}
    >
      <h2>{title}</h2>
      <p>{description}</p>
      <ul
        class={cn(
          "[&>li]:grid [&>li]:grid-cols-[min-content_1fr] [&>li]:items-center [&>li]:gap-6",
          "sm:[&>li]:gap-4",
          "[&>li>*]:first:text-3xl",
          "flex gap-2 flex-col",
          "font-bold",
        )}
      >
        <Accordion list={list} client:only="react" />
      </ul>
    </div> -->
  </div>
</section>
